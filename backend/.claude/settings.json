{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // \"\"' | { read -r f; [[ \"$f\" == *.java ]] && cd /Users/marcelobento/develop/back/java/strategyInvestment/backend && mvn compile -q; } 2>/dev/null || true",
            "timeout": 60,
            "statusMessage": "Compiling Java..."
          }
        ]
      }
    ]
  }
}
